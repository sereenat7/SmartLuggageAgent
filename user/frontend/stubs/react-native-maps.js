import React from 'react';
import { View } from 'react-native';

const MapView = (props) => <View {...props} />;
MapView.Marker = (props) => <View {...props} />;

export default MapView;
export { MapView };
